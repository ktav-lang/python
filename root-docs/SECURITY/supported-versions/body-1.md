>>>>> lang=en
## Supported versions

While this package is pre-1.0 only the **latest published minor** is
maintained. Security fixes land on `main` and ship in a PATCH
release within a few days.

| Version | Supported          |
|---------|--------------------|
| @@MINOR_LINE@@   | ✅                 |
| older   | ❌ — upgrade first |

>>>>> lang=ru
## Поддерживаемые версии

Пока пакет в pre-1.0, поддерживается только **последний опубликованный
минор**. Исправления безопасности попадают в `main` и выходят
PATCH-релизом в течение нескольких дней.

| Версия | Поддерживается |
|--------|----------------|
| @@MINOR_LINE@@ | ✅ |
| старше | ❌ — сперва обновитесь |

>>>>> lang=zh
## 支持的版本

本包仍处于 pre-1.0 阶段，仅维护**最新发布的次版本**。安全修复会进入
`main`，并在数日内以 PATCH 发布。

| 版本 | 支持 |
|------|------|
| @@MINOR_LINE@@ | ✅ |
| 更早 | ❌ —— 请先升级 |

