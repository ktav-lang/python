# Examples

Standalone scripts that exercise the package end-to-end. Run them after
`maturin develop`.

- [`basic.py`](basic.py) — parse a small service config, inspect the
  result, round-trip through `dumps` / `loads`.

```
python examples/basic.py
```
